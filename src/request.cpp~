#include "request.hpp"

using namespace nty;

Request::Request(request_data_type &&request_data)
  : _request_data(std::move(request_data))
{}
    
const Request::request_data_type&
Request::get_request_data() const noexcept
{
  return _request_data;
}
